name = "lastcall-keygen"
main = "mint.js"
compatibility_date = "2024-01-01"

# Deploy via CI (commit+push) — never wrangler from session
# Env vars set via CF dashboard: LICENSE_PRIVATE_KEY, STRIPE_WEBHOOK_SECRET
